{{#each posts as |post| }}
<div class="post">
  <h2><a href="/post/{{post.id}}">{{post.title}}</a></h2>
  <p>{{post.content}}</p>
  <p>Posted by {{post.user.name}} on {{format_date post.date_created}}</p>
</div>
{{/each}}
